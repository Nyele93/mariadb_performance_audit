
<h4 style="color:#72A0C1; font-family:verdana; font-size: 15px; font-weight: 200;">Innodb Buffer pool</h4>
<p style="font-family:Arial; font-size: 13px;">
The InnoDB buffer pool should generally be set to 60-80% of the available RAM when using InnoDB exclusively. Ideally, all InnoDB data and indexes should be able to fit within memory. 
</p>
<p style="font-family:Arial; font-size: 13px;">
A high value of the InnoDB Buffer Pool cache hit ratio suggests mariadb is able to efficiently handle page reads from the Buffer Pool. A low value would suggest otherwise – Inadequately sized Buffer Pool, etc.
</p>
<p style="font-family:Arial; font-size: 13px;">
A high value (>1)  of InnoDB Waits for Checkpoint will suggest an inadequately sized Buffer Pool, and database operations had to wait on a checkpoint to take place.
</p>

<style> .report-table th { background-color: #C0C0C0; color: #000066 }
 .example-table td:nth-child(1) { color: #000066; font-weight: bold; }
.example-table td:not(:first-child) { color: #0066FF; }
</style>

<table class="report-table" style="border: 1px solid black; border-collapse: collapse;">
<tr>
    {% for header in bufferpool_headers.split(",") %}
    <th style="border: 1px solid black; padding: 8px 16px; font-family:Arial; font-size: 13px;">{{ header }}</th>
    {% endfor %}
</tr>
{% for host in bufferpool_csv_file.list %}
<tr>
    {% for header in bufferpool_headers.split(",") %}
    <td style="border: 1px solid black; padding: 8px 16px; font-family:Arial; font-size: 13px;">{{ host[header] }}</td>
    {% endfor %}
</tr>
{% endfor %}
</table>

